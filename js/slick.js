AOS.init();

$('#banner').slick({
    autoplay:true,
    autoplaySpeed:1000,
    arrows:false,

});


$('#icon').slick({
    autoplay:true,
    autoplaySpeed:1000,
    slidesToShow:4,
    dots: true,
    arrows:false
});